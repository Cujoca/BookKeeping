package model.domain.files

import model.domain.objects.Transaction

import scala.collection.mutable

object FileHandler {
  
  def readFile(filePath: String): mutable.HashSet[Transaction] = {
    
  }

}
